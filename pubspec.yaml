name: shift_salary_calculator
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  provider: ^6.0.0
  shared_preferences: ^2.2.0
  intl: ^0.20.2
  table_calendar: ^3.2.0
  fl_chart: ^1.1.1
  http: ^1.6.0
  flutter_dotenv: ^6.0.0
  pdf: ^3.11.3
  printing: ^5.14.2
  share_plus: ^12.0.1
  path_provider: ^2.1.5
  in_app_purchase: ^3.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
